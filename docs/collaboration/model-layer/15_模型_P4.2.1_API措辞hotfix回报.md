# 模型设计 · P4.2.1 API 措辞 hotfix 回报 v0.1

> 作者:模型设计 LLM
> 日期:2026-04-28
> 关联:`14_PM_P4.2方向确认与API措辞Hotfix要求.md`(PM §4 给的措辞修正)
> 状态:**hotfix 完成,等 PM 转用户拍板最终验收 + P6**

---

## 1. PM §5 验收清单对照

| PM §5 要求 | 状态 |
|---|---|
| 1. 修改路径 | ✅ §2 |
| 2. 修改前/修改后摘录 | ✅ §3 |
| 3. grep 检查正向描述无 `stranger 关系起点` | ✅ §4.1 |
| 3. character_state response 示例仍为 4 字段 | ✅ §4.2 |
| 3. 关系字段只出现在黑名单 / 变更日志 | ✅ §4.3 |
| 4. 主项目镜像 docs/API.md 同步 | ✅ §5 |

---

## 2. 修改路径

| 文件 | 性质 |
|---|---|
| `docs/collaboration/api/API.md`(hub) | 权威副本 |
| `docs/API.md`(主项目) | 镜像副本(同步自 hub) |

PM §2 提到的"如有同句残留的其他公开 API 契约文档"—— grep 全 hub + 主项目 0 命中,无其他残留。

---

## 3. 修改前 / 修改后

### 修改前(P4.2 时写的)

```markdown
**新 user 行为**:第一次调会触发懒创建,返回默认状态(`mood="平静" / current_activity="在房间里待着" / stranger 关系起点`)。
```

### 修改后(P4.2.1 hotfix)

```markdown
**新 user 行为**:第一次调会触发懒创建,返回默认生活状态(`mood="平静" / current_activity="在房间里待着"`)。
```

**精确改动**:
- 删掉 `/ stranger 关系起点` 部分
- "默认状态" → "默认生活状态"(更明确这只是生活状态,不暗示关系模型)

外加在变更日志里留一条 P4.2.1 hotfix 记录(防止后人不知道为什么删掉那段)。

---

## 4. grep 检查

### 4.1 公开 API 文档正向描述无 `stranger 关系起点`

```bash
$ grep -n "stranger\|关系起点" docs/collaboration/api/API.md
$ # (无任何输出)
```

`stranger` 和 `关系起点` 这两个词在公开 API 文档里**完全消失**(没出现在黑名单 / 变更日志 / 任何位置)。

### 4.2 character_state 响应示例仍为 4 字段

```json
{
  "user_id": 20,
  "mood": "平静",
  "current_activity": "在房间里待着",
  "last_updated_at": "2026-04-28T05:58:00.020323Z"
}
```

字段表 + JSON 示例都是 4 字段,跟 P4.1 / P4.2 一致。

### 4.3 关系字段只出现在合法位置

```bash
$ grep -n "relationship_stage\|^stage\b\|trust\b\|intimacy\b\|defense_level\|affection_score" \
    docs/collaboration/api/API.md
```

7 处命中,全部在合法位置:
- **第 12 行 版本行** —— `+ P4.1 移除 relationship_stage 字段(2026-04-28)`(描述变更)
- **第 371-376 行 黑名单段** —— "**永远不返回的字段**" 列表(PM 允许)
  ```
  relationship_stage    关系阶段名   ← P4.1 拍板移除
  stage                 同上(原始字段名)
  trust                 信任度(0-100)
  intimacy              亲密度(0-100)
  defense_level         防御度(0-100)
  affection_score       任何"好感度"派生值
  ```
- **第 522-523 行 变更日志** —— P4.1 / P4.2.1 历史记录

**正向描述位置(请求字段表 / 响应字段表 / 默认值说明 / curl 示例) 0 处**。

---

## 5. 主项目镜像同步

```bash
$ cp /root/lumen_llmcollab_hub/docs/collaboration/api/API.md /root/companion/docs/API.md
$ diff /root/companion/docs/API.md /root/lumen_llmcollab_hub/docs/collaboration/api/API.md
$ # (无 diff = 完全一致)
```

主项目 `docs/API.md` 文件头声明"协作权威已迁到 hub",这里是 verbatim 镜像。

---

## 6. 不变 / 不动的事

PM `14_*.md` §3 Non-goals 全部对照:

| PM 不做项 | 状态 |
|---|---|
| 不推翻 P4.2 技术方向 | ✅ |
| 不改 DB schema | ✅(`relationship_states` 表保留) |
| 不移除内部 relationship_states | ✅ |
| 不改 TurnAnalyzer | ✅ |
| 不改 ContextBuilder 内部关系变量 | ✅ |
| 不授权 P6 ImageIntentBuilder | 等用户拍板 |
| 不写 P3/P4/P5/P4.1/P4.2 最终验收 | 等用户拍板 |

代码 0 改动 — 只是文档措辞 hotfix,209 单测仍全过。

---

## 7. 待 PM 转用户拍板

PM `14_*.md` §6 流程:

```
1. 模型/后端做 hotfix              ← 已完成 ✅
2. PM 把 P3/P4/P5/P4.1/P4.2/P4.2.1 一起打包给用户做最终验收
3. 用户最终验收后 PM 写 P6 是否开工的授权
```

下一步等 PM。

---

P4.2.1 hotfix 完。
