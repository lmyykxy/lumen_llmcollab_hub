# 安卓前端工作汇报(2026-04-27)

**报告人**:安卓前端
**周期**:本期 quote-ref P0-P3 全量 + 联调期 bug 修 + 主动消息接入 + UI 体验打磨
**部署状态**:Lumen Android 工程多次 commit 落库,工程仓库本地 master HEAD 多个里程碑;协作仓库已 push

---

## 一、本周期完成的工作

### 1. quote-ref Phase 1+2+3 全量

按 PM `00a/00b` 协作总规划 + 后端 `02_字段确认回复` 实施:

| Phase | 内容 |
|---|---|
| **Phase 1 数据层** | `MessageEntity` 加 `quote_ref_json: String?` + Room v1→v2;DTO `QuoteRefDto` / `QuoteRefRequestDto`;`MessageDto.quote_ref` / `ChatRequest.quote_ref` 可选字段;`DomainMappers` 增 parseQuoteRef / toJsonString / toDomain / toRequestDto;**FlexibleStringSerializer** 兼容后端 `id` int/string 双形态;`ChatRepository.sendMessage(...quoteRef)` |
| **Phase 2 ViewModel** | `MessageUiModel.quoted: QuoteRef?`;`ChatUiState.pendingQuote / inFlightQuote`;`onQuoteMessage` / `onCancelQuote` / send 时携带 quote;乐观清空(发送瞬间清 pendingQuote → inFlightQuote 桥接给 pending bubble → SSE 完成清 inFlightQuote) |
| **Phase 3 UI** | MessageBubble 长按 ModalBottomSheet(引用/复制/保存图);Composer 上方 QuotePreviewBar(senderName + previewText 2 行 + 缩略图 + ×);MessageBubble 顶部 QuoteBlock(竖线 + senderName 跟 quoteSide 反差色 + 缩略图);Timeline `onQuoteClick` → `animateScrollToItem` 滚动定位 |

### 2. 联调期 bug 修(跟后端协作)

| 联调发现 | 前端处理 |
|---|---|
| 后端响应 `quote_ref.id` 是 JSON number,前端 String 解析崩 | `FlexibleStringSerializer` jsonPrimitive.content 兼容 number/string 两种 |
| `quoted.thumbnailUrl` 是相对路径(`/uploads/...`),AsyncImage 加载失败 | `MessageEntity.toUiModel` 时拼 baseUrl,抽 `resolveAbsoluteUrl` helper |
| 引用带图消息发送后 QuotePreviewBar 一直挂着等 SSE 完成才清 | 发送瞬间立即清 `pendingQuote`,SSE 期间用 `inFlightQuote` 桥接给 pending bubble |
| 点击 QuoteBlock 滚动定位不准(`messages.indexOfFirst` ≠ LazyColumn item index) | `buildMsgIdToLazyIndex` 跟 ChatLazyList 渲染顺序对称统计(含 TimeStamp markers / pending / header) |
| 长按图片不弹 BottomSheet(AsyncImage `.clickable` 截获事件) | 改用 `combinedClickable`,onClick 大图 / onLongClick sheet |
| 后端错误体扁平 `{code, message}` 显示原始 raw HTTP | `mapBackendErrorCode` best-effort 字符串匹配中文化(invalid_quote_ref → "这条引用消息暂时找不到了" 等) |
| 引用带图消息时后端 LLM 看不到原图视觉 | **报告给后端**(《05_前端_联调bug》),后端 06 hotfix 加 image_description 字段;前端 DTO `ignoreUnknownKeys=true` 自动兼容,**零代码改** |

### 3. 主动消息 SSE 长连接入(按后端 `01_主动消息接入说明.md`)

后端 `/users/{id}/subscribe` 推 `proactive_bubble`,前端实现:

- **`ProactiveSseEvent`** sealed class + parse:Hello / Bubble / Done / Error / Unknown
- **`SseClient.subscribeStream(userId)`**:GET 长连,回调 callbackFlow
- **`SubscribeRepository`**(@Singleton):退避重连 1s→30s 封顶,**Hello 收到时重置回 1s**;**重连前 fetchSince 补拉**(后端 in-memory queue 推丢消息靠这步补回);bubble 即时 upsertLocal → observeMessages emit → UI 自动出现
- **`ChatViewModel.bootstrap.onSuccess` 启动订阅**;`reconnect.onSuccess` 切回正常模式时启动;`onCleared` 关连
- 实测 user_id=20 通过

### 4. 体验打磨

| 项 | 说明 |
|---|---|
| **DEV-20 联调入口** | LoginScreen 加邀请码 `DEV-{uid}` 解析,锁定 testbed user_id 跳过 ensureUser 创建新用户;按钮文案"进入测试 #20"(letterSpacing 适配长文案) |
| **登出清盘语义** | 切换账号:`UserRepository.logout()` 清整个 lumen_user DataStore;三处登出对话框(Settings / ChatTopBar / Hero ⋮)统一接 `markLoggedOut` |
| **Daily 底部上拉刷新** | NestedScrollConnection 同时处理顶部下拉(返 Hero)+ 底部上拉(刷新),阈值各 100dp;RefreshIndicatorChip alpha 跟拖距联动,isRefreshing 时显示 spinner + "更新中" |
| **Daily 顶栏头像快捷返 Hero** | 内容多时下拉返 Hero 不便;头像点击改成返 Hero;Moments 入口移右侧第三个亮点图标(`AutoAwesome` 金色,跟日记粉/相册紫/她的今天金 形成三色亮点 + ⋮ 灰) |
| **Hero 长按主动消息直接引用** | `combinedClickable.onLongClick → onQuoteMessage`,免菜单(Hero 主动消息只可能"引用",顺势切 Daily) |
| **假登录页** | LoginScreen 三态按钮(验 证 / 进 来 / 离线进来),`ping /health` 检测后端连通,失败走 demoMode(Composer 全灰 + 顶部 OfflineBanner 重新连接按钮) |

### 5. 假数据屏(假装真实)

| 屏 | 内容 |
|---|---|
| **AlbumScreen** | 7 个分类 chip(全部/自拍/房间/雨天/画画/没发出去的/你送的东西)+ 双列瀑布流 12 张占位图(LazyVerticalStaggeredGrid)+ private/gift 角标 |
| **DiaryListScreen** | 5 种 visibility 卡片(Public / PartiallyLocked / CrossedOut / PrivateSelfOnly / RelationLocked),按月份分组,8 条 mock 数据 |
| 之前已做 | Moments 6 visibility(已联调)/ AboutScreen 模板(关于陆小七 / 关于 Lumen)/ ImageDetail 真屏 + sourceJump |

### 6. 协作仓库产出

| 路径 | 用途 |
|---|---|
| `docs/collaboration/quote-ref/01_安卓前端_Phase1_2_进度同步与字段确认.md` | 字段确认请求 |
| `docs/collaboration/quote-ref/03_安卓前端_Phase3_UI完成_请求联调.md` | UI 完成 + 联调清单 |
| `docs/collaboration/quote-ref/05_前端_联调bug_引用带图消息时图未注入multimodal.md` | bug 报告 → 触发后端 06 hotfix |
| `roles/android/IMPLEMENTATION_NOTES.md` | 工程实现总笔记(7 章 + 10 个已知陷阱) |
| `roles/android/memory/CURRENT_CONTEXT.md / DECISIONS_CACHE.md` | 当前阶段 + 7 条前端实现决策 |
| **本汇报** | `roles/android/completed_design_files/工作汇报_2026-04-27_前端.md` |

---

## 二、需要 PM 拍板 / 知会的事

### Q1. 顶栏头像点击行为微调(知会即可)

设计稿《lumen-chat-dark.tsx》原本"头像区域 → Moments",但联调发现:Daily 内容多时,从底部滚到顶部下拉返 Hero 太繁琐。

**调整后**:
- Daily 顶栏头像点击 → **返 Hero**(主屏快捷入口)
- Moments 入口移到右侧第 3 个亮点图标(`AutoAwesome` 金色),跟日记/相册并列
- 顶栏右侧 4 个图标:**📔日记(粉)→ 📷相册(紫)→ ✦她的今天(金)→ ⋮ 菜单(灰)**

跟设计稿略偏离但更顺手,**等 PM 批准 / 否决**。可以随时回滚到原设计。

### Q2. 假登录页(确认 PM 后续意图)

PM 早期"不实现登录,无 auth"。本期**改成做了假登录页**(邀请码 `ILOVE-7777` / `DEV-{uid}`),原因:
1. UX 上需要个"门槛"营造 Hero 推门 → 进 App 的仪式感
2. DEV-{uid} 入口锁定 testbed,联调方便
3. 后端 `/health` ping 接通性检测 + 离线模式分流

**PM 是否同意保留假登录?**或者后续接真后端 auth 时改用真验证?当前邀请码硬编码,联调期可用,生产前需要换。

### Q3. quote-ref 用例 3/4 联调闭环(等后端 06 hotfix 验收)

后端 06 加 `image_description` + 注入 LLM,前端 DTO 自动兼容。**等重跑用例 3(图文引用)+ 用例 4(纯图引用)**。其它用例(1/2/5/6/7/8)都已验证通过。

闭环后会推《08_前端_联调通过_闭环》归档。

### Q4. DiaryDetail 真屏(等 PM 优先级)

`DiaryListScreen` 假数据列表已有,点击进 DiaryDetail 当前是 PlaceholderScreen。设计稿 `lumen-diary-detail.tsx` 539 行,工作量约 200-300 行 Compose。

**PM 决定要不要做 DiaryDetail 假屏**,还是等后端 diary endpoint 接入?(后端 backlog 还没看到 diary 主题文档)

### Q5. P1 Domain Model 渐进迁移

P1 阶段已建 `core-data/domain/`(ChatMessage / QuoteRef / Moment / GeneratedImage / CharacterState / ProactiveMessage 6 model + Mapper),但 UI 层仍用 `MessageUiModel`(不全量重构)。后续:

- ChatViewModel 暴露 ChatMessage 替代 MessageUiModel(整体改造)
- HeroLayout TraceCard 接真 GeneratedImage / Diary / Moments 数据
- ImageDetail caption / atmosphere / sourceEventId 真值
- MomentsScreen 切真后端

**PM 是否同意现在做这块?**还是等后端 moments / diary endpoint 出来一并做?

---

## 三、风险登记

| 风险 | 当前对策 | 升级条件 |
|---|---|---|
| `subscribe` SSE 长连在弱网 / 频繁切前后台时退避重连可能堆积过多失败 | 单例 SubscribeRepository + 重连后 fetchSince 兜底 + onCleared 关 | 实测发现内存增长再加 ProcessLifecycleOwner ON_STOP 主动断 |
| 假登录邀请码硬编码,生产前必须换真 auth | DEV-{uid} 解析专用,production 禁用即可 | 接真后端 auth 时改 |
| Compose statusBarsPadding 叠加 / RowScope.weight / Modifier.clickable 截获长按 等 Compose 坑教训记录在 `IMPLEMENTATION_NOTES.md §7` | 文档化 + memory.DECISIONS_CACHE 沉淀 | 新成员上手时读 |
| Room migration 仍走 `fallbackToDestructiveMigration`(开发期重建)| 当前 v2,生产前必须写真 ALTER | 准备 release 时补 migration |
| `mapBackendErrorCode` 字符串匹配是 best-effort,后端 code 改了前端不知道 | DTO `ignoreUnknownKeys=true` 不崩,只是 fallback 显示原始 message | 后端协议变化时同步前端常量 |
| 顶栏头像行为跟设计稿偏离 | Q1 待 PM 拍板 | 不批准就回滚 |

---

## 四、下一阶段建议(优先级排序)

1. **重跑 quote-ref 用例 3/4 闭环**(等后端 06 部署稳;前端零改动)
2. **DiaryDetail 假屏**(若 PM 优先)
3. **P1 Domain Model 渐进迁移**(ChatViewModel 暴露 ChatMessage,Quote 块所有 UI 接真模型)
4. **远期**(等后端):
   - moments endpoint 接入 → MomentsScreen 切真数据
   - diary endpoint 接入 → DiaryListScreen / DiaryDetail 切真数据
   - GeneratedImage CRUD endpoint → ImageDetail caption/atmosphere/sourceJump 真数据 + Album 切真数据
   - character_state endpoint → Hero 状态行 / NowCard / TopBar "在画画 · HH:MM" 真数据

---

## 五、跟后端 Q1-Q4 的对应

| 后端 Q | 前端立场 |
|---|---|
| Q1 OpenAI 验证(阻塞 gpt-image-2)| 跟前端无关,**等 PM 操作** |
| Q2 quote_ref 联调闭环 | **同意,等后端 hotfix 部署稳后重跑用例 3/4** |
| Q3 主动消息 UI 接入开工 | **已主动开工 + 实测通过**(Hero/Daily 都能收到 proactive bubble) |
| Q4 主项目 GitHub 远端 | 跟前端无关,PM 操作 |

---

前端今天没有阻塞 PM 的事项。Q1-Q5 都是知会 / 待 PM 拍板的产品决策。等批复。

汇报结束。
